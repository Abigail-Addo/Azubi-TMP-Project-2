{/* courses */ }
{/* <div className='flex items-center gap-2 flex-wrap'>
                                                    {track.courses[0]?.stacks && track.courses[0].stacks.length > 0 ? (
                                                        track.courses[0].stacks.map((stack, index) => (
                                                            <p key={index} className='text-[#175CD3] bg-[#E9F3FB] rounded-full py-1 px-4'>{stack}</p>
                                                        ))
                                                    ) : (
                                                        <p className="text-gray-400 text-sm">No stacks</p>
                                                    )}
                                                </div> */}